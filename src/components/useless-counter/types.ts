export type ConfettiPiece = {
  id: number;
  left: number;
  delay: number;
  rotate: number;
  color: string;
  drift: number;
  duration: number;
};

export type CountryStat = { country: string; count: number };
